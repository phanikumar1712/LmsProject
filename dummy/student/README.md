# Student Dummy Data

Placeholder for sample files used by student-facing tooling or testing
(student import CSV/XLSX lives under **admin** — students are provisioned by
admins, e.g. `dummy/admin/students/`).

If you need sample student records for local testing, see the bulk user import
templates (`/api/users/students/template`).
