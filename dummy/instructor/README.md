# Instructor Dummy Data

Placeholder for sample import files used by instructor flows:

- **Course import** (`dummy/instructor/courses/`) — course CSV/XLSX for the
  course bulk-import template (headers: title, category, level, etc.)
- **Lesson / section imports** if the platform adds them

Check `backend/src/controllers/coursesController.js` (course import) and the
`Course_Import_Template.xlsx` download for the accepted columns.
