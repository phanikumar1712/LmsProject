Course-builder reference data. `sample-course.json` lists every lesson type
(video, pdf, text, quiz, assignment, coding, document, audio, external) and
the fields the builder expects per lesson. Use it as a checklist when
creating a test course at `/instructor/create-course`.
